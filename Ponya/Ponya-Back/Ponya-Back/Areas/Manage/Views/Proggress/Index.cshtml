@model List<Proggress>
@{
    ViewData["Title"] = "Index";
    Layout = "~/Areas/Manage/Views/Shared/_ManageLayout.cshtml";
    int order = 0;
}

<div class="container-fluid">
    <div class="container-fluid d-flex justify-content-between align-items-center">
        <h1>Proggress</h1>
        <a asp-action="Create" asp-controller="Proggress" class="btn btn-primary">Create</a>
    </div>
    <table class="table">
        <thead>
            <tr>
                <th scope="col">No</th>
                <th scope="col">Name</th>
                
                <th scope="col">Raiting </th>
                <th scope="col"> Is Deleted</th>
                
                <th scope="col">Status</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                order++;
                <tr>
                    <th scope="row">@order</th>
                    <td>@item.Name</td>
                    <td>@item.Raiting</td>
                    <td>@(item.IsDeleted==true?"Deleted":"On Active")</td>
                    
                    <td>
                        <a asp-action="Edit" asp-controller="Proggress" asp-route-id="@item.Id" class="btn btn-warning">Edit</a>
                        <a asp-action="Delete" asp-controller="Proggress" asp-route-id="@item.Id" class="btn btn-secondary btn-sweet-delete">Delete</a>
                        <a asp-action="PermaDelete" asp-controller="Proggress" asp-route-id="@item.Id" class="btn btn-danger btn-sweet-delete">Perma-Delete</a>
                    </td>
                </tr>
            }

        </tbody>
    </table>
</div>

